name: iOS CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Xcode 16
        run: sudo xcode-select -switch /Applications/Xcode_16.x.x.app

    - name: Build and Test
      run: |
        xcodebuild clean test \
        -scheme SampleCITest \
        -destination 'platform=iOS Simulator,name=iPhone 14,OS=18.0' \
        CODE_SIGNING_ALLOWED=NO

    - name: Test Result
      if: failure()
      run: |
        echo "Test failed"
